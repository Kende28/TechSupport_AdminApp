<Window x:Class="TechSupport.AdminApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Login"
        Width="328" Height="253"
        WindowStartupLocation="CenterScreen">

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Admin Login" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

		<StackPanel Grid.Row="1" Margin="0,8,0,0">
			<TextBlock Text="Username"/>
			<TextBox Text="{Binding Username}" Margin="0,0,0,8"/>
			<TextBlock Text="Password"/>
			<PasswordBox x:Name="PasswordBox" Margin="0,0,0,8"/>
		</StackPanel>

		<Button Grid.Row="2" Content="Login" Width="80" Height="30" HorizontalAlignment="Center"
                Command="{Binding LoginCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Click="PasswordSync"/>
	</Grid>
</Window>
